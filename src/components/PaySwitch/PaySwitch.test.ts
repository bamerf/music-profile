import * as React from "react";

import PaySwitch from "./PaySwitch";

describe("<PaySwitch>", () => {
  it("renders without error", () => {
    expect(true).toEqual(true);
    // const wrapper = render(<PaySwitch />);

    // expect(wrapper).toBeTruthy();
  });

  it.todo("Add more tests");
});
