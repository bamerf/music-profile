import * as React from "react";

import PayRadio from "./PayRadio";

describe("<PayRadio>", () => {
  it("renders without error", () => {
    expect(true).toEqual(true);
    // const wrapper = render(<PayRadio />);
    // expect(wrapper).toBeTruthy();
  });
  it.todo("Add more tests");
});
