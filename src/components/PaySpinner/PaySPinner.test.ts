import * as React from "react";

import PaySpinner from "./PaySpinner";

describe("<PaySpinner>", () => {
  it("renders without error", () => {
    expect(true).toEqual(true);
    // const wrapper = render(<PaySpinner />);

    // expect(wrapper).toBeTruthy();
  });

  it.todo("Add more tests");
});
